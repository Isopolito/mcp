{
  "name": "codex-bridge",
  "version": "1.0.0",
  "description": "MCP Bridge Server for Codex CLI integration with Gemini CLI",
  "main": "dist/index.js",
  "bin": {
    "codex-bridge": "./dist/codex-bridge"
  },
  "scripts": {
    "build": "tsc && npm run make-executable",
    "make-executable": "esbuild dist/index.js --bundle --platform=node --format=cjs --outfile=dist/codex-bridge && sed -i '1i#!/usr/bin/env node' dist/codex-bridge && chmod +x dist/codex-bridge",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "esbuild": "^0.19.12"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["mcp", "codex", "openai", "gemini", "cli", "bridge"],
  "author": "Your Name",
  "license": "MIT"
}